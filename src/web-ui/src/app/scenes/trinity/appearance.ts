import type { AppearanceSurfaceDescriptor } from '@/infrastructure/appearance/types';

export const trinityAppearanceDescriptor: AppearanceSurfaceDescriptor = {
  id: 'trinity',
  parts: [{ id: 'root' }, { id: 'scroll' }, { id: 'content' }],
};